# --- Database ---
DB_HOST=db
DB_USER=root
DB_PASSWORD=brightbuy
DB_NAME=BrightBuy
DB_DIALECT=mysql
DB_PORT=3306

# --- App ---
APP_PORT=8081

# --- Auth ---
# Generate a long random string
JWT_SECRET=replace-with-long-random-secret

# --- Stripe ---
STRIPE_SECRET_KEY=sk_test_replace
STRIPE_WEBHOOK_SECRET=whsec_replace

# --- AWS S3 (for image uploads) ---
AWS_ACCESS_KEY_ID=AKIA_REPLACE
AWS_SECRET_ACCESS_KEY=REPLACE_ME
AWS_REGION=ap-south-1
S3_BUCKET_NAME=brightbuy

# Notes:
# - Do not commit real secrets. Copy this file to .env and fill real values locally or in deployment.
# - For Docker Compose dev, DB_HOST should be 'db' to match the service name.
# - For production, set DB_HOST to your DB host/IP and use strong credentials.
