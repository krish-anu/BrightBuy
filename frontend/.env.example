# Vite environment variables must start with VITE_

# Backend API base URL
VITE_BACKEND_BASE_URL=http://localhost:8081

# Example: if backend is behind a domain
# VITE_BACKEND_BASE_URL=https://api.brightbuy.example.com
